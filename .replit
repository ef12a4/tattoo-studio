{
  "name": "tattoo-studio",
  "run": {
    "exec": [
      "pip install -r requirements.txt",
      "python init_db.py",
      "gunicorn --bind 0.0.0.0:$PORT wsgi:app"
    ]
  },
  "environment": {
    "FLASK_ENV": "production",
    "PORT": "$PORT"
  }
}
